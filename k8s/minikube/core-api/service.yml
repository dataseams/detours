apiVersion: v1
kind: Service
metadata:
  name: core-api
  labels:
    app: core-api
spec:
  type: LoadBalancer
  selector:
    app: core-api
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000
